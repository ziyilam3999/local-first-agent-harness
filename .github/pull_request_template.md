## Summary

<!-- What does this change and why? -->

## Test plan

<!-- How did you verify it? `ruff check src/ tests/` + `pytest tests/ -q`, and any
     manual `lfah run` / `--dry-run` you exercised. -->

## Checklist

- [ ] `ruff check src/ tests/` passes
- [ ] `pytest tests/ -q` passes
- [ ] Conventional Commits message (`feat(...)`, `fix(...)`, `docs: ...`)
- [ ] Added a bullet under `## Unreleased` in `CHANGELOG.md` (if user-facing)
